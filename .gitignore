# Claude Code local settings (machine-specific)
.claude/settings.local.json

# Submitted coursework — kept locally, deliberately out of the public repo
# (scrubbed from history 2026-09-02; do not re-add)
assignments/

# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
*.swp
.vscode/
.idea/

# Secrets — never commit. .env.example is the tracked template.
.env
forecaster/.env

# Local run output: traces, the sent-item ledger, nightly logs
forecaster/data/

# Python
.venv/
.pytest_cache/
__pycache__/
*.py[cod]
.ruff_cache/

# Course notes and program writing — kept locally, out of the public repo (2026-09-02)
modules/
writing/

# Session guidance — local only (2026-09-02)
CLAUDE.md
